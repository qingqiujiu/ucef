## UCEF code-chain analysis

For deep Java business-chain excavation or multi-scenario comparison, use the `ucef` OpenCode skill. Persist durable conclusions in UCEF facts/evidence rather than relying on conversation memory. Use `.ucef/site/index.html` as the default human-readable system explorer. Treat generated HTML and Obsidian as views, not the technical source of truth.
