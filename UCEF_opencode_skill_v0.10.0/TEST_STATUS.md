# UCEF v0.10.0 test status

Validation covers legacy import compatibility plus the direct three-layer runtime, hard task budgets, idempotent receipts, role/tool boundaries, independent workspace safety, incremental reader HTML, deterministic SVG sequence projection, content-addressed redacted JSON artifacts, independent artifact pages, method/field evidence, durable v0.9 memory import, and cross-scenario comparison projection.

Run:

```bash
python -m unittest discover -s tests -v
```

Validated on 2026-08-25: 22 tests passed.

Final release count and archive hash are recorded after packaging in the repository release notes.
