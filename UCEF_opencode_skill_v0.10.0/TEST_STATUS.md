# UCEF v0.10.0 test status

Validation covers legacy import compatibility plus the direct three-layer runtime, timeout continuation without replanning, self-describing task capsules, one-correction submission limits, typed Agent tools, hard task budgets, idempotent receipts, role/tool boundaries, independent workspace safety, dirty legacy block rendering, incremental reader HTML, on-demand technical evidence, deterministic SVG sequence projection, content-addressed redacted JSON artifacts, independent artifact pages, method/field evidence, durable v0.9 memory import, and cross-scenario comparison projection.

Run:

```bash
python -m unittest discover -s tests -v
```

Validated on 2026-08-25: 29 tests passed. Coverage includes a real UTF-8 `submit_block` subprocess running under an intentionally legacy Windows code-page environment, plus UTF-8 BOM workspace and artifact inputs.

Final release count and archive hash are recorded after packaging in the repository release notes.
