# UCEF 1.0.0 test status

The complete suite contains **53 passing tests**. Validation covers the adaptive primary Agent and optional read-only focused probe, compact skill metadata, version consistency, resumable semantic-milestone sessions, shared cross-scenario business entities, one-time entity renaming, scenario-isolated configuration values and branch outcomes, real multi-branch decisions, evidenced branch convergence, unresolved/confirmed production configuration, P0 field provenance, incremental field journeys, ordinary DTO-copy folding, persistence mapping, external request/response consumption, full-envelope targeted-context budgets, soft token budgets, source-relative evidence validation, credential redaction, cross-linked offline adaptive HTML, and deterministic sequence, flow, system-relationship, state-transition, and lineage SVG diagrams.

The historical suite continues to exercise v0.9/v0.10 imports, direct-run continuation, durable memory, Windows-code-page UTF-8 process boundaries, BOM JSON, independent workspace safety, redacted artifacts, legacy method/field evidence, and existing HTML compatibility.

Run from the 1.0 package root:

```bash
python -m unittest discover -s tests -v
```

The same suite preserves historical runtime and workspace compatibility while exercising the complete 1.0 command-line workflow.
